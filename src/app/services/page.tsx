import Link from 'next/link';import {services} from '@/lib/services';
export const metadata={title:'Computer, Phone, Gaming & IT Services Waterbury CT',description:'Explore OnyxTech Solutions services for computer repair, phones, gaming systems, data recovery, networking, POS, printers, electronics and business IT.'};
export default function Services(){return <main id="main"><section className="serviceHero"><div className="container"><div className="eyebrow">OnyxTech Solutions Services</div><h1>Computer, Phone, Gaming & IT Services</h1><p className="pageIntro">Choose the service that matches your problem. Every service has its own local SEO landing page, starting-price guidance, FAQs and booking path.</p></div></section><section><div className="container grid">{services.map(s=><article className="card serviceCard" key={s.slug}><h2>{s.shortName}</h2><p>{s.description}</p>{s.startingPrice&&<p className="price">Starting at ${s.startingPrice}</p>}<Link className="btn secondary" href={`/services/${s.slug}`}>View Service</Link></article>)}</div></section></main>}
